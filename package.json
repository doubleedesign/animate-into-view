{
	"name": "@doubleedesign/animate-into-view",
	"version": "1.0.0",
	"type": "module",
	"description": "Using IntersectionObserver and CSS animations to have page elements make grand entrances.",
	"author": "Leesa Ward",
	"homepage": "https://doubleedesign.github.io/animate-into-view/",
	"repository": {
		"type": "git",
		"url": "https://github.com/doubleedesign/animate-into-view"
	},
	"license": "MIT",
	"scripts": {
		"sass:dev": "sass --watch src:dist docs",
		"build": "tsc && sass src:dist demo && cp -r dist demo",
		"build:docs": "cd docusaurus && yarn build && mv build/* ../docs && cp -r ../demo ../docs/"
	},
	"devDependencies": {
		"eslint": "^8.34.0",
		"sass": "^1.58.3",
		"typescript": "^4.9.5"
	}
}
